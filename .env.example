# Supabase — copy this file to .env.local and fill in real values from
# Supabase Dashboard → Project Settings → API.
# Both vars MUST be prefixed with VITE_ so Vite exposes them to the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
