/**
 * Cuántos ejercicios tiene la práctica de Comunicaciones ATC, para quien no
 * debe cargar la práctica entera (el panel, el hub, Ingreso a aerolínea).
 *
 * El guion (`comunicacionesPracticaEjemplos.ts`) lleva cada transmisión con su
 * texto y su explicación; aquí solo va el número, como `aeropuertosConteo.ts`.
 *
 * Si cambia el guion, `leccionesConteo.test.ts` falla y dice qué poner.
 */
export const CM_PRACTICA_CONTEO = 126
