# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vercel
.vercel

# Local env files (keep .env.example tracked)
.env
.env.*
!.env.example

# Supabase CLI runtime (locally cached versions, linked-project.json, etc.)
supabase/.temp/
supabase/.branches/

# Brand asset packs (raw zips dropped during onboarding — extract into brand/ then ignore the archive)
*_logo_pack.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*.local

# Private content batches — NUNCA commitear preguntas en plano al repo.
# Solo el template .example va al control de versiones.
private-batches/*
!private-batches/_template.json.example
!private-batches/README.md
.claude/
